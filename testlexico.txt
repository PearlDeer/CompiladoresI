
main {
    int x, y, z;
    float a, b, c;

    suma = 99;
    x = 23.32;
    x = 32;
    y = 5 + 2 - 1;
    z = y + 5;
    y = y + 9;
    a = 21.0 + 5 - 1 / 3 * 2 + 33 - 4;
    x = (8 - 3) * (4 / 2);
    y = 5 + 3 - 2 * 4 / 7 - 9;
    z = 4 / 2 + 20 * 4;
    y = 9.54;

    if (a > b) then
        y = b + 3;
    else
        if (suma > 2) then
            b = 2.2;
        else
            b = 10.0;
        end
        y = y + 1;
    end

    a++;
    c--;
    x++;
    suma = x ^ 5;
do
    while (x > 7)
        y = (y + 1) * 2 + 1;
        x = 6 + 8 / 9 * 8 / 3;
        cin >> x;
        mas = 36 / 7;
    end
while(x==4 &&);

        while (z < 100)
            z = z + 10;
            cout << z * 2 + 5;
        end

    while (y == 0)
        cin >> mas;
        cout << 69 + 65 / 8;
    end

    do
        while(x > 2)
             cout << "Hola Mundo";
        end
        cout << (x + y * 2 - z / 3);
    while (x > 1);
}